# kemo-graph 参考文档 (GPT版)

先把这三件事分开看:

```text
知识图谱: 解决"知识之间是什么关系"
RAG:      解决"用户问题时去哪里找材料"
Agent:    解决"现在该做什么"
```

---

## 一、kemo-graph 是什么

kemo-graph 不是向量库，也不是 LLM。它的核心任务只有一句话:

```text
给定一个问题，先走图谱命中+扩展，再走向量召回+精排，融合后把路径和候选内容交给调用方。
```

和 kemo-agent 的边界:

```text
kemo-graph = 一次 ingest，双索引（图+向量），联合查询，权重可调，rerank 兜底
kemo-agent = 决定什么时候塞、怎么问、拿到结果后做什么
```

---

## 二、当前架构

### 1. 目录结构

```
kemo-graph/
├── config/               # 配置
├── core/                 # 核心抽象（ingest 解析、数据模型、融合策略）
├── data/
│   ├── Graph/            # 图存储：节点、边、路径
│   └── RAG/              # 向量存储：chunk、embedding 索引
├── external/             # 文档入口 + 远程加工支持
├── provider/
│   ├── tools/            # API 工具集
│   └── engine.py        # API 调用引擎（embedding / rerank / LLM）
├── web/                  # Web UI（图谱可视化、检索预览、DSL 编辑）
├── graph.py              # 图谱操作入口
├── rag.py                # RAG 操作入口
├── start.py              # CLI 融合调度入口
├── start_web.py          # Web 服务入口
└── 参考文档-gpt.md
```

### 2. 为什么是这个结构

| 设计决策 | 原因 |
|----------|------|
| `data/Graph/` 和 `data/RAG/` 物理分离 | 图和 RAG 的生命周期不同。删文档时 RAG 删 chunk 重刷向量就行，图要处理引用计数和残余边。强行合并会让 RAG 的灵活性被图的严谨性拖死 |
| `graph.py` + `rag.py` 双入口 | 调用链天然是先图后 RAG，分开抽象易维护。各自独立可用，也能被 start.py 编排融合 |
| `start.py` 做融合调度 | 分离不等于不融合。start 负责协调 graph + rag，做 α 权重融合，输出联合结果 |
| `core/` 放核心抽象 | ingest 逻辑、数据模型、融合策略统一在 core 里，不展平成多个顶层目录，符合自己的组织习惯 |
| `external/` 放文档 + 远程加工 | 数据源入口和外部加工能力统一管理，不混入核心逻辑 |
| `provider/` 保留 LLM 调用 | 制作知识图谱、向量生成、重排序时需要 LLM 辅助，不只是 embedding/rerank |
| `web/` + `start_web.py` | 图谱可视化、RAG 检索结果预览、DSL 编辑器需要前端展示，不只是被当库调 |

### 3. 调用链

```
用户问题
    │
    ▼
  start.py（融合调度）
    │
    ├──► graph.py（先走图谱）
    │     ├── 节点命中
    │     ├── 邻居扩展
    │     ├── 路径查询
    │     └── 权重/深度/类型过滤
    │
    └──► rag.py（再走 RAG）
          ├── 向量召回 top_k
          └── rerank 精排
    │
    ▼
  α 权重融合 → 输出结构化结果
    │
    ▼
  交给 kemo-agent
```

---

## 三、融合但不混同

### 逻辑关联、物理分离

核心矛盾:

```text
融合点: ingest 一次解析，同时产出节点/边和 chunk，共享 node_id 做关联键
分离点: 图存储和向量存储物理分开，各自管各自的生命周期
```

为什么不能合并存储:

| 维度 | Graph | RAG |
|------|-------|-----|
| 删除文档 | 成本高（节点可能被多文档引用，需引用计数+边清理） | 成本低（删 chunk 重刷向量即可） |
| 冗余容忍 | 不容忍重复节点（否则路径查询会乱） | 容忍冗余（多 chunk 靠 rerank 精排） |
| 更新粒度 | 概念级（跨文档抽象） | 文档级（线性切片） |

一句话:

```text
逻辑上 node_id 天然绑定，物理上各管各的，查询时再融合。
```

---

## 四、数据模型

### 1. 节点 (Node)

```json
{
  "id": "RAG",
  "keyword": "RAG",
  "summary": "检索增强生成",
  "aliases": ["Retrieval-Augmented Generation"],
  "tags": ["检索", "生成", "LLM"],
  "source": "rag_notes.md"
}
```

### 2. 边 (Edge)

```json
{
  "source": "RAG",
  "relation": "包含",
  "target": "向量检索",
  "weight": 0.9,
  "source_file": "kg.md"
}
```

### 3. 图谱 DSL

最小 DSL:

```text
{主题:节点名:[摘要]}
{主题:源节点-[关系|权重]->目标节点}
```

示例:

```text
{AI检索:RAG:[检索增强生成，先检索再生成。]}
{AI检索:向量检索:[把文本转成向量做相似度查找。]}
{AI检索:RAG-[包含|0.9]->向量检索}
{AI检索:RAG-[包含|0.8]->知识图谱}
{AI检索:向量检索-[依赖|0.9]->Embedding}
```

别名扩展:

```text
{AI检索:RAG@alias:[Retrieval-Augmented Generation, 检索增强生成]}
{AI检索:RAG@source:[notes/rag.md]}
```

---

## 五、向量检索和重排序（最小概念）

### 向量模型 (Embedding)

把一段文字变成一串数字。意思越相近，数字坐标越接近:

```
「今天天气真好」 → [0.23, -0.87, 1.04, ...]
「外面阳光明媚」 → [0.25, -0.84, ...]   ← 和上面很近
「财务报表分析」 → [-1.30, 0.92, ...]   ← 和上面很远
```

```text
向量检索 = 海选快筛（便宜、快）
```

### 重排序 (Rerank)

向量检索返回 top_k，但不一定最准。重排序模型逐对精比，重新打分:

```text
重排序 = 决赛精判（贵一点、更准）
```

两个都是调 API，不需要本地跑模型。

---

## 六、权重融合

联合查询时每条候选有两个分数:

```text
graph_score ← 来自边权、路径深度、关系类型
embed_score ← 来自向量相似度
```

融合公式:

```text
final_score = α × graph_score + (1-α) × embed_score
```

α 不写死，按查询场景动态调:

| 场景 | α 倾向 | 原因 |
|------|:---:|------|
| "XX 和 YY 什么关系" | 偏高 0.7 | 结构关系更关键 |
| "有没有关于 XX 的资料" | 偏低 0.3 | 语义匹配更关键 |
| "我之前写的那个..." | 均衡 0.5 | 两者都重要 |

α 作为查询参数传入，由调用方（kemo-agent）根据问题类型决定。

---

## 七、完整数据流

```
                         用户塞内容
                      (DSL / Markdown / JSON)
                              │
                              ▼
                    ┌─────────────────┐
                    │    external/     │  接收文档
                    └────────┬────────┘
                             │
                              ▼
                    ┌─────────────────┐
                    │     core/       │  ingest 一次解析
                    └───┬─────────┬───┘
                        │         │
                节点 + 边        chunk 文本
                        │         │
                        ▼         ▼
                  ┌──────────┐ ┌──────────┐
                  │data/Graph│ │ data/RAG │  物理分离
                  │ 图存储   │ │ 向量索引 │
                  │ 路径查询 │ │ top_k    │
                  └────┬─────┘ └────┬─────┘
                       │            │
                       └─────┬──────┘
                             │  逻辑关联: node_id
                             ▼
                    ┌─────────────────┐
                    │  start.py       │
                    │  graph.py → 命中 │
                    │  rag.py  → 召回  │
                    │  α 融合 + rerank │
                    └────────┬────────┘
                             ▼
                    ┌─────────────────┐
                    │   输出结果       │
                    │  交给 kemo-agent │
                    └─────────────────┘
```

---

## 八、API 依赖

| 环节 | 调什么 | 成本 | 必要性 |
|------|--------|------|:---:|
| embedding | embedding API | 极低 | ✅ 骨架级 |
| rerank | rerank API | 中等 | ✅ 已加入 |
| LLM | LLM API | 中等 | ✅ 知识图谱构建/校验需要 |

向量模型和重排序可以接外部 API，不必自建。LLM 用于辅助知识图谱制作（如自动抽取节点/边、校验关系合理性），不用于文本生成。

---

## 九、输出结构

```json
{
  "query": "RAG 和知识图谱怎么结合？",
  "matched_nodes": ["RAG"],
  "expanded_nodes": ["RAG", "向量检索", "知识图谱"],
  "paths": [
    ["RAG"],
    ["RAG", "向量检索"],
    ["RAG", "知识图谱"]
  ],
  "fusion_alpha": 0.5,
  "results": [
    {
      "id": "chunk_001",
      "graph_score": 0.90,
      "embed_score": 0.78,
      "final_score": 0.84,
      "node_id": "RAG",
      "source": "rag.md",
      "content": "..."
    }
  ]
}
```

---

## 十、图谱最少要会查什么

- 节点命中
- 邻居扩展
- 路径查询
- 子图查询
- 权重过滤
- 深度控制
- 类型过滤

---

## 十一、不做什么（排除清单）

先不要把下面这些混进来:

- 文档解析（PDF/Word）→ 上游的事
- 文本生成 → kemo-agent 的事
- 多轮记忆 → kemo-agent 的事
- 自动知识抽取 → MVP 先用 DSL 手动控制，LLM 仅辅助
- 评估层（召回率/准确率）→ MVP 之后再考虑

---

## 十二、项目定位

```text
kemo-graph 是 kemo-agent 的 global_expand 外部拓展能力，独立项目。
不继承不绑定，通过 API 通信。
```

与 kemo-agent 的关系:

```text
kemo-agent: 调用方，决定什么时候塞、怎么问、拿到结果做什么
kemo-graph: 被调方，负责存储、检索、融合、返回
```

与架构哲学的关系:

```text
每个项目边界清晰，通过 API 通信，不共享代码库。
不把知识图谱塞进 kemo-agent，不把 RAG 做成子模块。
```

---

## 十三、最小结论

```text
存储物理分离，逻辑 node_id 绑定
调用先图后 RAG，start 做融合
α 权重可调，rerank 兜底
LLM 辅助建图，不做生成
Web 端可视化，不只是库
```